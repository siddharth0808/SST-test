{
  "name": "amplify-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sst:deploy": "sst deploy --stage qa --print-logs",

    "sst:dev": "sst dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sst": "3.14.11"
  }
}
